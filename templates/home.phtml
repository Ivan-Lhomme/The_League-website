
    <header>
        <h2>Tout sur la League</h2>
        <p>Les players, les teams et l'actu des matchs</p>
    </header>
    <main>
        <div class = "a_la_une">
            <h3>La team a la une</h3>
            <div class = "pres">
                <h3>feature angry owls here</h3>
            </div>
        </div>
        <div class="players_a_la_une">
            <h3>Les players à la une</h3>

            <?php foreach ($featuredPlayers as $player) : ?>
                <div class="<?= $player->getNickname() ?>">
                    <?php $image = $player->getImage(); ?>
                    <img src="<?= $image->getUrl() ?>" alt="<?= $image->getAlt() ?>">
                    <h3><?= $player->getNickname() ?></h3>
                </div>
            <?php endforeach; ?>

        </div>

        <div class="match_a_la_une">
            <h3>Le dernier Match</h3>
            <p>date ici</p>
            <div class="match_a_la_une">
                <div class="team1">
                    <?php foreach ($featuredTeams as $team) : ?>
                        <div>
                            <?php $teamImage = $team->getLogo(); ?>
                            <img src="<?= $teamImage->getUrl() ?>" alt="<?= $teamImage->getAlt() ?>">
                            <p><?= $team->getName() ?></p>
                            <button onclick="window.location.href = '?route=team&teamId=<?= $team->getId() ?>';">VOIR ÉQUIPE <?= $team->getName() ?></button>
                        </div>
                    <?php endforeach; ?>

                </div>

    </main>
